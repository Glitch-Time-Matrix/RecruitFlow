import { ComingSoon } from "@/components/dashboard/ComingSoon";

export const metadata = { title: "Applications" };

export default function ApplicationsPage() {
  return (
    <ComingSoon
      title="Applications"
      description="The recruitment pipeline — move candidates through stages from applied to hired."
      phase="Phase 5"
    />
  );
}
